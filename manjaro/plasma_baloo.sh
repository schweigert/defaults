
sudo rm /etc/xdg/autostart/baloo_file.desktop
sudo touch /etc/xdg/autostart/baloo_file.desktop

# Replate `~/.config/ballofilerc` for:
```
[Basic Settings]
Indexing-Enabled=false

[General]
first run=false
```
